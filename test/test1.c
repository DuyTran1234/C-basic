#include <stdio.h>
#include <string.h>
int main()
{
    printf("\n");
    int custAge = 38;
    if (custAge > 21 && custAge < 35)
        printf("They are welcone\n\n ");
    else
        printf("They are not welcone\n\n");

    printf("! turns a true into false : %d\n\n", !1);
}